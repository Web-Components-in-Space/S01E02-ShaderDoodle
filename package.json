{
  "name": "spacedoodle",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "lit": "^2.2.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.30",
    "typescript": "^4.6.2"
  },
  "scripts": {
    "tsc": "./node_modules/.bin/tsc --project tsconfig.json",
    "tsc-watch": "./node_modules/.bin/tsc --project tsconfig.json --watch",
    "serve": "npm run tsc-watch & ./node_modules/.bin/wds --watch"
  }
}
